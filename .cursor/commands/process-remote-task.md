# 处理远程任务

请读取项目根目录下的 `agent-tasks/pending.md` 文件，执行其中所有**未完成**的待处理任务（即没有 `[已完成]` 标记的任务）。

对每个任务：
1. 理解并执行
2. 将执行结果简要记录
3. 在对应任务前加上 `[已完成]` 标记
4. 将执行摘要追加到 `agent-tasks/results.md`

若 `agent-tasks` 目录或 `pending.md` 不存在，则回复「暂无待处理任务」。
